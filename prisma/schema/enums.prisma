enum Role {
    ADMIN
    MEMBER
}

enum UserStatus {
    ACTIVE
    BLOCKED
    DELETED
}

enum IdeaStatus {
    DRAFT
    PENDING
    APPROVED
    REJECTED
    UNPUBLISHED
}

enum VoteType {
    UPVOTE
    DOWNVOTE
}
